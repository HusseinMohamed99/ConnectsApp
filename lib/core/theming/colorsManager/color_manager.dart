import 'package:flutter/material.dart';

/// Manages the primary and secondary colors used throughout the app.
class ColorManager {
  static const Color accentBlueColor = Color(
    0xFFD6E6FF,
  );
    static const Color textBlackColor = Color(0xFF1F1E1E);
    static const Color mainBlack = Color(0xFF000000); 
  static const Color mainBlue = Color(0xFF2132B2); 


}
