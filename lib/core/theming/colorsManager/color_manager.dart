import 'package:flutter/material.dart';

/// Manages the primary and secondary colors used throughout the app.
class ColorManager {
  static const Color accentBlueColor = Color(
    0xFFD6E6FF,
  ); // Soft background blue tint

  static const Color accentPurpleColor = Color(
    0xFFE5E6FF,
  ); // Light purple background (shared w/ lavender)

  static const Color blackColor = Color(0xFF222222); // Secondary black
  static const Color blueTabColor = Color(0xFFE8F4F9); // Light blue tab
  static const Color darkBlue = Color(
    0xFF342E85,
  ); // Darker variation of brand blue

  static const Color darkBlueTabColor = Color(
    0xFFD8E9F3,
  ); // Slightly darker tab background

  static const Color darkColor = Color(0xFF101440); // App dark background
  static const Color darkGreyColor = Color(0xFFBCC1C6); // Text/input border
  static const Color greenAccentColor = Color(
    0xFFEAFAF1,
  ); // Greenish-tinted background (success)

  static const Color greenColor = Color(0xFF27AE60); // Success green
  static const Color greyColor = Color(0xFFEAEAEA); // Light gray surface
  // === Tab/Surface Backgrounds ===
  static const Color greyTabColor = Color(0xFFF7F9FC); // Light background tab

  static const Color gridGreenColor = Color(
    0xFFD1F7EB,
  ); // Success green background/grid highlight

  static const Color headerColor = Color(0xFF1C1E1B); // Large header text
  static const Color indigoColor = Color(
    0xFF38379C,
  ); // Indigo branding variation

  // === Highlight & Decorative ===
  static const Color lightGreyColor = Color(
    0xFFE0E0E0,
  ); // Input border / dividers

  // === Brand Primary Colors ===

  // === Neutral Colors ===
  static const Color mainWhite = Color(0xFFFFFFFF); // App white

  static const Color newMainBlue = Color(
    0xFF100CA1,
  ); // Deep blue/purple tone for headers

  static const Color paragraphColor = Color(0xFF666666); // Paragraph body text
  // === Accent Colors ===
  static const Color popColor = Color(
    0xFF05D99B,
  ); // Vibrant mint accent (used in buttons/success)

  static const Color purpleColor = Color(0xFFA07FEC); // Base purple accent
  static const Color redAccentColor = Color(
    0xFFFEE3E5,
  ); // Light red background (error)

  // === Status Colors ===
  static const Color redColor = Color(0xFFF64650); // Error red

  static const Color shadeColor = Color(0xFFD9D9D9); // Shadow/hover divider
  static const Color smallHeaderColor = Color(0xFF333333); // Smaller headings
  static const Color subHeaderColor = Color(0xFF4D4D4D); // Subheader text

  // === Themed ===
  static const Color tealColor = Color(0xFF29ACA8); // Main teal tone

  // === Text Colors ===
  static const Color textGreyColor = Color(0xFF8D8F8F); // Secondary gray text

  static const Color yellowColor = Color(0xFFFDB827); // Branding yellow/accent



  // NEEDED
    static const Color textBlackColor = Color(0xFF1F1E1E); // Primary black text
    static const Color mainBlack = Color(0xFF000000); // App black
  static const Color textGrey = Color(0xFF1C1E1B); // Tertiary gray text
  static const Color mainBlue = Color(0xFF2132B2); // Main brand blue


}
