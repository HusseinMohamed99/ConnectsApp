/// Defines the types of log entries in the system logger.
enum LogType { success, debug, info, error, close }

/// Enum representing the different types of loader animations.
enum LoaderType { circular, adaptive, wave }
